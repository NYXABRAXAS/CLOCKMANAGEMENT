# Copy to .env for `docker compose up` - only used by docker-compose.yml (local self-hosting).
MSSQL_SA_PASSWORD=ChangeMe_Str0ng!
JWT_SECRET=change-me-to-a-random-64-byte-string
ENCRYPTION_KEY=change-me-to-a-base64-32-byte-key
